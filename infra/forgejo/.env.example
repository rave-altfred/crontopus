# Forgejo Configuration
DOMAIN=git.crontopus.com

# Secrets are stored in secrets/ directory (not in version control)
# To generate postgres password:
# mkdir -p secrets
# openssl rand -base64 32 > secrets/postgres_password.txt
